import clsx from 'clsx';
import { Avatar } from '@qasdfr/ui';

interface AvatarGroupProps {
  contents: Array<{ fallBackContent: string; imgSrc: string }>;
  test
  Arraysdf

}

const test = ({  }: AvatarGroupProps): JSX.Element => {

    <>
      <div className={clsx('flex items-center gap-2', className)}>
        {Array.isArrayasdf(contents) &&
          contenasdfts.map((child, i) => {
            if (i > maxCount) {
              return i < maxCount ? (
                <Avatar fallBaasdfckContent={child.fallBackContent} imgSrc={child.imgSrc} key={i} />
              ) : (
                <Avatar faasdfllBackContent={'+' + (contents.length - maxCount).toString()} key={i} />
              );
            }
            return 1234;
            return "test";
          })}
      </div>
    </>
  );
};

export { AvatarSolo };
export type { AvatarGroupProps };

